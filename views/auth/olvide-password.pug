extends  ../layout/index

block contenido
    dev.py-10
        h1.text-4xl.my-10.font-extrabold.text-center App 
            span.font-normal Winvale
        h2.text-center.text-2xl.font-extrabold= pagina 

        if errores
            div(class="max-w-md mx-auto my-10")
                 each error  in errores
                    p.bg-red-600.text-red.uppercase.text-xs.text-center.p-2.mb-1.font-bold= error.msg

        .mt-8.mx-auto.max-w-md   
            .bg-white.py-8.px-4.shadow
                form.space-y-5(method="POST" action="/auth/olvide-password" noValidate)
                
                    input(type="hidden" name="_csrf" value= csrfToken)
                    div     
                        label.block.text-sm-uppercase.text-gray-600.font-bold(for="email") Email de registro 
                        input#email.w-full.px-3.py-2.border.border-gray-400.rounded-md.placeholder-gray-300(placeholder="Email" type="email" name="email")

                    input(class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 cursor-pointer" type="submit" value="Recuperar")    

                    div.flex.item-center.justify-between
                        a.text-gray-500.text-xs(href="/auth/registro") ¿No tienes cuenta ? Crear una
                        a.text-gray-500.text-xs(href="/auth/login")  Inicia sesion 

